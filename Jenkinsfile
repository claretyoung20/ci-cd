pipeline {

    agent any

    stages{

        stage('Build') {

            steps{
                script {
                    def name = "nne"
                    if(name == "nne") {
                        echo "Hi, $name"
                    } else {
                        echo "Hi, no body"
                    }
                }
            }

        }

    }
}